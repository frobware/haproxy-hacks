#!/usr/bin/env bash

thisdir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
. "${thisdir}/common.sh"

: "${HOSTIP:=$(dig +short $(hostname))}"
: "${DOMAIN:=$domain}"

for name in $(backend_names_sorted); do
    echo "$HOSTIP ${name}.${domain}"
done
