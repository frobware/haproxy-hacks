apiVersion: v1
kind: List
items:
- apiVersion: v1
  kind: Service
  metadata:
    name: hello-openshift
  spec:
    selector:
      app: hello-openshift
    ports:
      - port: 8080
        name: 8080-http
        targetPort: 8080
        protocol: TCP
- apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: hello-openshift
  spec:
    replicas: 1
    template:
      metadata:
        name: hello-openshift
        labels:
          app: hello-openshift
      spec:
        containers:
        - image: openshift/hello-openshift
          name: hello
          ports:
            - name: "http"
              containerPort: 8080
      imagePullPolicy: IfNotPresent
    selector:
      matchLabels:
        app: hello-openshift
        
